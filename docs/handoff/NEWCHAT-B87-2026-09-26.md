# Continue EKA2L1 NATIVEBOOT2 from B87

Repository: `phai-nguyen/Eka2l1_bot_menu_simbiam`
Branch: `nativeboot2-current`

## Current action

B87 CALLHANDLINGUICAPTURE1 retargets the read-only RSC capture to the exact resource owner proven from the complete RM-356 `SYM.RPKG`: `Z:\\resource\\apps\\callhandlingui.r01/.r96`. The change is diagnostic only. FASTBUILD has been triggered by the commit; install nothing until the green result and IPA are confirmed.

## Evidence

- Complete `SYM.RPKG`: 8,030 entries, 134,540,934 bytes, SHA-256 `bc41496abc8d4c87de976b65cadfb922b4dfd9583a0dbcf35b7e4bff23eb008f`.
- Both callhandlingui locale files use UID3 `0x1099B`, signature base `0x1099B000`, contain 46 resources, and own resource ID `0x1099B02D` (entry 45).
- B85 remains Telephone CONE14 / `NoResourceFileForId`. The resource exists in firmware; outstanding guest question is why PhoneUI has not opened/registered its owner at the failing lookup.
- Supplied 14:05–14:07 +0700 logs and iOS crash are from B85. B86 FASTBUILD completed later at 14:18 +0700, so the B85 crash does not satisfy the user's B86-reproduction condition.
- Keep iOS exit/teardown and guest resource-registration behavior unchanged in B87.

## After FASTBUILD

1. Verify run is green, binary contains the B87 marker, and the unsigned IPA artifact is present.
2. Give the user the IPA and only the short install/test steps on iPhone.
3. Analyze the B87 marker and any callhandlingui open/dump against the offline RSC hashes above.
4. If B87 shows no owner-file open, trace the PhoneUI resource registration path before a functional registration change.
