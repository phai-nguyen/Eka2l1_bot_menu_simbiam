# Continue EKA2L1 NATIVEBOOT2 from B86

Repository: `phai-nguyen/Eka2l1_bot_menu_simbiam`
Branch: `nativeboot2-current`

Read `docs/handoff/history/B86-PHONEUIRESDUMPFIX1.md` for the B85 log/crash evidence and exact scope.

## Findings to preserve

- B85 still reaches Telephone CONE14, reason 14, with resource ID `0x1099B02D` at 14:06:27.417.
- `VPbkCntModelRes.r01` opened twice shortly before the panic. Its ownership remains unproven.
- B85 produced no candidate-RSC dump because it compared the lowercased path to a mixed-case constant.
- The B85 iOS crash report is a host-app SIGSEGV at address `0x3` while `exitEmulator` transitions to `showAppsScreen` and UIKit handles remote keyboard/window cleanup. Root cause is still unverified.

## B86 purpose and boundary

B86 fixes the case-sensitive mismatch and requires runtime marker `[NBOOT2][PHONEUI_RESID_CANDIDATE_DUMP_B86]` in FASTBUILD's compiled binary. It still only captures `z:\\resource\\VPbkCntModelRes.r01` through a separate read-only handle for offline RSC inspection. It does not alter emulator exit or teardown, resource registration, CONE14, or boot behavior.

After FASTBUILD is green, the user installs and tests B86 on iPhone. Observe both whether the emulator exits via the Vietnamese “Menu trò chơi” without crashing to Home, and whether B86 logs contain `PHONEUI_RESID_CANDIDATE_DUMP_B86` begin/data/end entries. If the same exit crash reproduces, the user's explicit instruction is to fix it then. Do not preemptively patch that path in B86.

The user wants Codex to run cloud FASTBUILD autonomously. Continue through build/test harness errors where possible, and provide the IPA only after a green build.
