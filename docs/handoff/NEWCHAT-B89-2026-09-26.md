# Continue EKA2L1 NATIVEBOOT2 from B89

Repository: `phai-nguyen/Eka2l1_bot_menu_simbiam`  
Branch: `nativeboot2-current`  
Goal: reach the RM-356 Symbian Home/menu. The user authorizes targeted startup
workarounds toward that goal.

Read [CURRENT.md](CURRENT.md) and
[history/B89-FATALSTATEBYPASS1.md](history/B89-FATALSTATEBYPASS1.md).

## B88 device result

The exact Telephone `CONE 14` panic now exits cleanly, and the user's B88 run
did not crash back to iOS Home. However, SYSSTART still writes global P&S state
`101 -> 116` (FatalStartupError), and the video remains on
`Phone start-up failed. Contact the retailer.`. The menu is not reached.

## B89 change

Once only, after the exact B88 Telephone exit, map SYSSTART's exact global-state
P&S write `101 -> 116` to `109 NormalRfOn`. This targets release of the normal
post-critical firmware startup list, including Menu3. Build status is pending
until the FASTBUILD workflow on `nativeboot2-current` completes.

Device success requires seeing the Symbian Home/menu. Continue from any next
guest startup gate shown by the B89 log/video.
