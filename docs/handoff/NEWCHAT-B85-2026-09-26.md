# Continue EKA2L1 NATIVEBOOT2 from B85

Repository: `phai-nguyen/Eka2l1_bot_menu_simbiam`
Branch: `nativeboot2-current`
Base: B84 commit `22d8270570096ce2a3b9fdb1a5b2ce7fc0983e02`

Read `docs/handoff/history/B85-PHONEUIRESIDOWNER1.md` first for the B84 log evidence and B85 scope.

## Current state

B84 still reaches the Telephone/CONE14 panic. Its owner label `callhandlingui.r01` was hard-coded, so it cannot be treated as evidence. The B84 CPU register snapshot gives resource ID `0x1099B02D`; `VPbkCntModelRes.r01` is a candidate observed shortly before the panic while VPbk modules appear in stack frames, but ownership is unverified.

## B85

B85 keeps boot/panic/resource registration unchanged. It corrects the CPU register and owner labels, then dumps the candidate VPbk RSC through a separate read-only handle (max 262144 bytes) as hex chunks for offline decoding. FASTBUILD must verify marker `[NBOOT2][PHONEUI_RESID_RSC_CANDIDATE_DUMP]` in the binary.

After build, check FASTBUILD and IPA artifact. Do not describe B85 as a boot fix. Use B85's captured RSC together with source/resource decoding to identify the owner, then propose the smallest evidence-backed behavior fix. Device validation still requires user-provided logs because no Symbian device is attached to this build runner.

## User constraints

The user primarily has an iPhone and wants Codex to read submitted logs, make the next version, and run the cloud FASTBUILD autonomously. Continue through build and repair failures where possible; do not ask the user to operate GitHub Actions unless the runner itself is blocked.
