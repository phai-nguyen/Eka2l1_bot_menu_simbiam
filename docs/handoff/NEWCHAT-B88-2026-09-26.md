# Continue EKA2L1 NATIVEBOOT2 from B88

Repository: `phai-nguyen/Eka2l1_bot_menu_simbiam`
Branch: `nativeboot2-current`
Goal: reach the RM-356 Symbian Home/menu. The user has explicitly authorized targeted boot-path workarounds; the previous diagnostic-only limit is superseded.

## Current state

- B87 CALLHANDLINGUICAPTURE1 FASTBUILD is GREEN; its unsigned IPA and audit were verified.
- Complete RM-356 SYM.RPKG proves resource `0x1099B02D` belongs to `callhandlingui.r01/.r96`.
- Earlier device evidence: Telephone UID3 `0x100058B3` panics CONE 14 on that resource lookup and sends startup to state 116 (“Phone start-up failed”).

## B88

B88 changes only that exact failure at EKA2L1's thread-kill boundary. It ends Telephone with a clean termination result (type `terminate`, reason 0) so Starter can continue. All unrelated panics remain unchanged. This bypasses the failure; it does not fix the resource registration or preserve Phone functionality.

Local B88 contract tests passed (2/2). FASTBUILD is triggered by the commit; verify B28 baseline, manifest tests, compile/link, B88 binary marker, and IPA/audit before providing the build.

## Device acceptance

Install B88 using the same signing method used for previous unsigned builds. Success requires an actual Symbian Home/menu screen. If it still remains at the Nokia splash or Phone startup error, record the full logs; proceed to a direct firmware shell launch fallback. Keep the user's separate report of an iOS crash while exiting distinct from guest boot failure.

