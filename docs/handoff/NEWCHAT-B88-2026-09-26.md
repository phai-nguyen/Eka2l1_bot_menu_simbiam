# Continue EKA2L1 NATIVEBOOT2 from B88

Repository: `phai-nguyen/Eka2l1_bot_menu_simbiam`
Branch: `nativeboot2-current`
Goal: reach the RM-356 Symbian Home/menu. The user has authorized targeted boot-path workarounds; the previous diagnostic-only limit is superseded.

## Current state

- B87 CALLHANDLINGUICAPTURE1 FASTBUILD is GREEN; its unsigned IPA and audit were verified.
- Complete RM-356 SYM.RPKG proves resource `0x1099B02D` belongs to `callhandlingui.r01/.r96`.
- Earlier device evidence: Telephone UID3 `0x100058B3` panics CONE 14 on that resource lookup and sends startup to state 116 (“Phone start-up failed”).

## B88 — built and verified

B88 changes only that exact failure at EKA2L1's thread-kill boundary. It ends Telephone with a clean termination result (type `terminate`, category `None`, reason 0) so Starter can continue. All unrelated panics remain unchanged. This bypasses the failure; it does not fix resource registration, and Telephone will be unavailable.

- FASTBUILD #270 / run `36229862894`: GREEN.
- B28 baseline, all manifest regressions, iOS compile/link, B88 binary marker, IPA packaging, and audit: PASS.
- IPA SHA-256: `3756273e80febcf43f48926877f9d810f734f5f49ee970d2fcbe94fa4783aa15`.
- Run: https://github.com/phai-nguyen/Eka2l1_bot_menu_simbiam/actions/runs/36229862894

## Device acceptance

Install B88 using the same signing method used for previous unsigned builds. Success requires an actual Symbian Home/menu screen. If it remains at the Nokia splash or the Phone startup error, record the full logs; proceed to a direct firmware shell-launch fallback. Keep iOS exit crashes distinct from guest boot failure.
